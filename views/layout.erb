<!DOCTYPE html>
<html lang="en">

  <head>
    <title>MyContacts App</title>
    <meta charset="utf-8">
  </head>

  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>

    <%== yield %>
  </body>

</html>
